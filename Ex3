V = []
menor, maior, a, b = 0, 0, 0, 0
for i in range(15):
    n = int(input())
    V.append(n)
    if i == 0:
        maior = menor = V[i]
    if V[i] > maior:
        maior = V[i]
        a = i
    if menor > V[i]:
        menor = V[i]
        b = i
print(maior, a)
print(menor, b)
