V = [0 for i in range(20)]
menor, maior = 0, 0
for i in range(20):
    V[i] = int(input())
    if i == 0:
        maior = menor = V[i]
    if V[i] > maior:
        maior = V[i]
    if menor > V[i]:
        menor = V[i]
print(V)
print(maior)
print(menor)
